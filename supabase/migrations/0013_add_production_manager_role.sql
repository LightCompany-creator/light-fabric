-- =====================================================
-- 0013 · Роль «начальник производства»
-- =====================================================
-- Отдельная роль (не флаг на foreman): начальник производства
-- получает отдельный логин и отдельный набор функций (заказы),
-- начальник цеха работает под своим обычным логином foreman.
-- Один и тот же человек (например, Арут) может иметь оба логина.
-- =====================================================

alter type user_role add value if not exists 'production_manager';
